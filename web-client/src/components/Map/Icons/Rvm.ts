import L from "leaflet";
import rawWebpIcon from "../../../assets/plastic-bottle.webp";

const rvmIcon = L.icon({
  iconUrl: rawWebpIcon,
  iconSize: [32, 32],
  iconAnchor: [16, 16],
});

export default rvmIcon;
